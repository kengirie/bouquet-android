package io.github.kengirie.bouquet.ui.theme

import androidx.compose.material3.Typography

/**
 * Bouquet uses Material3 default typography. The desktop's Space Grotesk /
 * Inter / Manrope stack is a "nice to have" but custom fonts add APK weight,
 * runtime cost, and offline complexity that we'd rather avoid.
 */
internal val BouquetTypography = Typography()
